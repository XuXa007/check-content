type User {
    id: ID!
    username: String!
    email: String!
    role: String!
}

extend type Query {
    users: [User]
}

extend type Mutation {
    addUser(userDTO: AddUserDTO!): User
}

input AddUserDTO {
    username: String!
    email: String!
    role: String!
}